import styled from 'styled-components';

export const StyledSubheading = styled.h3`
  font-size: 1.5rem;
  font-weight: 500;
  line-height: 1.6;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`;
