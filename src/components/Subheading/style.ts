import styled from 'styled-components';

export const StyledSubheading = styled.h3`
  font-size: 1rem;
  font-weight: 700;
  line-height: 1.6;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`;
